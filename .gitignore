# ── Build artifacts ──────────────────────────────────────
target/
*.pdb
*.exe
*.dll
*.so
*.dylib

# ── VSCode Local History ──────────────────────────────────
.history-files/
.history/

# ── SurrealDB / RocksDB ──────────────────────────────────
selene_memories.db/
*.db/
*.db

# ── Dados neurais binários ────────────────────────────────
*.hlx
*.bin
selene_qtable.bin
selene_spikes.hlx

# ── Backups automáticos (limpar_db.py) ───────────────────
*.bak_*.json

# ── Logs e crash reports ─────────────────────────────────
*.log
selene_crash_report.txt
dashboard_log.txt

# ── Arquivos temporários ─────────────────────────────────
nvme_buffer.bin
*.tmp
*.bak

# ── Ambientes virtuais Python ────────────────────────────
Scripts/
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ── Biblioteca de treinamento (PDFs grandes) ─────────────
Selene_Brain_2.0/selene_kernel/livros/

# ── Arquivos 3D (face.obj/mtl) ───────────────────────────
Selene_Brain_2.0/selene_kernel/data/

# ── IDE / OS ─────────────────────────────────────────────
.vscode/settings.json
.idea/
Thumbs.db
.DS_Store

# ── Estado de execução (gerado em runtime) ───────────────
selene_ego.json
selene_hippo_ltp.json
selene_manifest.json
selene_memoria_ego.json
memoria_categorias.json

# ── Arquivo .old (legado movido para triagem) ─────────────
Selene_Brain_2.0/selene_kernel/.old/

# ── V4.6.1 — Limpeza: artefactos gerados + estado de aprendizagem ─────────
# Build do app Android (gerado pelo Gradle — nunca versionar)
selene_android/app/build/
selene_android/**/build/
selene_android/.gradle/
# Estado de aprendizagem da Selene (gerado/atualizado em runtime)
selene_autobiografia.json
selene_hypotheses.json
selene_linguagem.json
selene_ontogeny.json
selene_swap_state.json
selene_lexicon.json
# Artefactos de pesquisa/triagem mantidos só localmente
files/
files.zip
*.zip
test_audio_*.json
# Settings locais do Claude Code
.claude/settings.local.json
